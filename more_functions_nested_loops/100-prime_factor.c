#include "main.h"
/**
 * print_number - check the code
 *@n: given
 *
 * Return: Always 0.
 */
void print_number(int n)
{
  int i = 0;
  while (n > 0)
    {
      int k = n % 10
      _putchar(k);
      n = n / 10;
  
}